# คัดลอกไฟล์นี้เป็น .env:  cp .env.example .env

# พอร์ตบน host ที่จะใช้เปิด FiftyOne App (เว็บ) — เปลี่ยนได้ถ้าชนกับบริการอื่น
FIFTYONE_APP_PORT=5151

# หมายเหตุ: ชุดข้อมูล (datasets/) และโมเดล (models/) อยู่ในตัว repo แล้ว
# ถ้ายังไม่มี ให้รัน:  ../scripts/setup-assets.sh   (จาก repo root)
